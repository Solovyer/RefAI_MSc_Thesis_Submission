# Evaluation

This folder contains evaluation workbooks and comparison outputs, including entity-level gold-standard comparison and quality-control analysis.

The main comparison strategy is place/entity-level first, then field-level comparison for matched places. This avoids misleading row-by-row comparison when records have been merged, split or manually reinterpreted.
