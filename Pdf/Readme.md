# PDF

This folder contains the seven selected test PDFs used in the thesis evaluation batch.

The original local project also contains a much larger historical PDF archive. That full archive is not included here to keep the deposition package manageable. The included files are the PDFs referenced by `BATCH_PDF_NAMES` in the current pipeline configuration.
