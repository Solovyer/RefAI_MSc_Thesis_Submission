# templates

This folder contains output templates used by the pipeline to determine the main output columns.

`Output_Template_1979.xlsx` is the active template referenced by the current configuration.
